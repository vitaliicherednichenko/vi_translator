<div id="<%= dom_id collection %>" class="card shadow-sm">
  <div class="card-body">
    <h5 class="card-title mb-3">
      <%= collection.name %>
      <% if collection.language %>
        <span class="badge bg-success"><%= collection.language.code.upcase %></span>
      <% end %>
    </h5>
    <dl class="row mb-0">
      <dt class="col-sm-3"><%= t("collections.field_description") %></dt>
      <dd class="col-sm-9"><%= collection.description.presence || tag.span(t("common.none"), class: "text-muted") %></dd>

      <dt class="col-sm-3"><%= t("collections.field_owner") %></dt>
      <dd class="col-sm-9"><%= collection.user&.name.presence || tag.span(t("common.none"), class: "text-muted") %></dd>

      <dt class="col-sm-3"><%= t("collections.field_language") %></dt>
      <dd class="col-sm-9"><%= collection.language&.name || tag.span(t("common.none"), class: "text-muted") %></dd>
    </dl>
  </div>
</div>
