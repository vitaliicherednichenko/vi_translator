<% content_for :title, t("collections.edit_title") %>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <h1 class="mb-4"><%= t("collections.edit_title") %></h1>
    <div class="card shadow-sm">
      <div class="card-body">
        <%= render "form", collection: @collection %>
      </div>
    </div>
    <div class="mt-3 d-flex gap-3">
      <%= link_to t("collections.show_this"), @collection, class: "btn btn-link px-0" %>
      <%= link_to t("collections.back_to_collections_plain"), collections_path, class: "btn btn-link px-0" %>
    </div>
  </div>
</div>
