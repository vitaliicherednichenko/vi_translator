<% content_for :title, t("languages.edit_title") %>

<div class="row justify-content-center">
  <div class="col-lg-8">
    <h1 class="mb-4"><%= t("languages.edit_title") %></h1>
    <div class="card shadow-sm">
      <div class="card-body">
        <%= render "form", language: @language %>
      </div>
    </div>
    <div class="mt-3 d-flex gap-3">
      <%= link_to t("languages.show_this"), @language, class: "btn btn-link px-0" %>
      <%= link_to t("languages.back_to_languages_plain"), languages_path, class: "btn btn-link px-0" %>
    </div>
  </div>
</div>
