<% content_for :title, t("cards.new_title") %>

<div class="row justify-content-center">
  <div class="col-lg-10">
    <h1 class="mb-4"><%= t("cards.new_title") %></h1>
    <div class="card shadow-sm">
      <div class="card-body">
        <%= render "form", card: @card %>
      </div>
    </div>
    <div class="mt-3">
      <%= link_to t("cards.back_to_cards"), collection_cards_path(@collection), class: "btn btn-link px-0" %>
    </div>
  </div>
</div>
